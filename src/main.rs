use std::sync::Arc;
use std::sync::atomic::{AtomicPtr, Ordering};
use std::thread;
use std::thread::JoinHandle;
use std::time::Duration;
use rand::Rng;
use std::cell::RefMut;
use std::rc::Rc;


fn main() {
    // let t = std::ptr::null<usize>();
    RefMut::map()
}
